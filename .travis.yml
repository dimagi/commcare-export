language: python
python:
 - "2.6"
 - "2.7"
 - "3.3"
install: 
 - "pip install . --use-mirrors"
 - "pip install psycopg2 sqlalchemy alembic pytest --use-mirrors"
script: py.test
services:
 - postgres
notifications:
  email:
    - kknowles@dimagi.com
